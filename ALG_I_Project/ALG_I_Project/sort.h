#pragma once

#include <vector>
using namespace std;

/**
 * @brief Třídí soubor celých čísel metodou výběru.
 * 
 * @param set Soubor celých čísel, který se má seřadit.
 */
void selectionSort(vector<int>& set);
